function Tin_2 = mixer_inverse_model(Qin, Qout, Tin, Tout)
    if Qin >= Qout
        throw(MException("mixer_inverse_model:invalid_inputs", "Qin (%.2f) cannot be larger than Qout (%.2f)", ))
    Tin_2 = (Tout*Qout - Tin*Qin) / (Qout - Qin);
end
